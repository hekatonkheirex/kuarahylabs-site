---
import type { JsonLd } from "../../lib/structured-data";
interface Props { data: JsonLd; }
const { data } = Astro.props;
---
<script is:inline type="application/ld+json" set:html={JSON.stringify(data).replace(/</g, "\\u003c")} />
