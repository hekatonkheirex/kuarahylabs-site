# Kuarahy Labs

Static landing page for Kuarahy Labs, built with Astro + TypeScript.

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build   # outputs to dist/
npm run preview
```

## TODO before launch

- Drop the real logo at `public/logo.png` (referenced by the hero and favicon/OG tags).
- Swap `public/pawrecord-icon.svg` for the real PawRecord app icon.
- Add the PawRecord Google Play URL in `src/pages/index.astro` (currently a disabled "Coming soon" button).
- Add social links to the Contact section in `src/pages/index.astro` once you have them.
